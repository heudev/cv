/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['enes.run'], // Add the hostname here
    },
    // Other config options
};

module.exports = nextConfig;
